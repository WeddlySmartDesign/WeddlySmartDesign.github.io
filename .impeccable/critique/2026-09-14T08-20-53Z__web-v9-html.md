---
target: web-v9.html
total_score: 24
max_score: 32
na_heuristics: 7,10
p0_count: 0
p1_count: 4
target_identity: "file:/home/user/WeddlySmartDesign.github.io/web-v9.html"
target_fingerprint: "sha256:2f4cc2b0d9e8181b62da4b24e9a9d1ad5ef9e030a34b8e55ef284aa22af83769"
target_path: /home/user/WeddlySmartDesign.github.io/web-v9.html
timestamp: 2026-09-14T08-20-53Z
slug: web-v9-html
---
Method: dual-agent (A: a74f2ae596ccf9ac1 · B: a90115a7118f17d26)

#### Design Health Score

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 3/4 | Two different "waiting" strings for the same state |
| 2 | Match System / Real World | 4/4 | Solid — real Bodas.net/WhatsApp/plano vocabulary throughout |
| 3 | User Control and Freedom | 2/4 | 8686px scroll, no jump-to-chapter nav, only "Volver arriba" |
| 4 | Consistency and Standards | 3/4 | Internally consistent, but tap targets fall under mobile norms |
| 5 | Error Prevention | 3/4 | Few inputs; main risk is mis-tapping 29.5px chips 6px apart |
| 6 | Recognition Rather Than Recall | 4/4 | Captions restate selected chip's context clearly |
| 7 | Flexibility and Efficiency | n/a | Landing page — no power-user path expected |
| 8 | Aesthetic and Minimalist Design | 3/4 | Clean per-section, but identical structure repeated 6× |
| 9 | Error Recovery | 2/4 | "No se ha podido cargar X." — flat text, no icon, no retry |
| 10 | Help and Documentation | n/a | Landing page, not a help system |
| **Total** | | **24/32** | **Good (75%)** |

#### Design Specificity Verdict

**LLM assessment**: Split verdict. The copy is genuinely specific to this product — "Sentar es mover. No rellenar un campo.", the Bodas.net/WhatsApp/floor-plan detail — this is not generic SaaS copy with nouns swapped. But the *visual system* is: strip the copy from any of the 6 feature sections and the skeleton (eyebrow → serif headline with one italic word → muted paragraph → horizontal chip row → device-in-black-bezel → italic caption) is the standard "SaaS feature tour" template wearing a Cormorant-and-terracotta costume. Zero photography or wedding-specific visual motif anywhere on the page — only `<img>` is the logo. This is exactly the risk you named: a visitor forms an impression before reading a word.

**Deterministic scan**: CLI detector found **89 findings** (`impeccable detect --json`, exit 2) across 9 rule types: undersized-ui-text ×56, tiny-text ×7, low-contrast ×6, cramped-padding ×6, nested-cards ×6, buried-raster ×4, all-caps-body ×2, kicker-above-heading ×1, cream-palette ×1. Browser live-scan corroborated with 79 findings (same rule set; minor count discrepancy vs. CLI not resolved, likely a render-pass/dedup difference, not a new defect class).

- The **undersized-ui-text/tiny-text/cramped-padding** cluster directly corroborates Assessment A's manual measurement of `.guide button` at 29.5px tall with 6px gaps.
- **low-contrast**: `--muted` (#6f6459) on `--blush` (#ede0d3) = 4.4:1, and `--sage` (#5f7a5e) on `--blush` = 3.7:1 — both fail the 4.5:1 AA floor. This is a background Assessment A's own spot-check of `--muted` didn't hit (it checked cream/ink instances, which pass at 5.1:1/8.96:1) — a real, additional, fixable finding.
- **kicker-above-heading**: the craft floor bans this outright ("no brief earns it back... delete the label and let the heading speak"). The detector counted 1 hit, but the `.eyebrow` pattern sits above literally every section heading on the page (hero, all 6 chapters, sync section, final CTA) — likely under-counted by dedup, but structurally the single biggest contributor to the "generic template" read alongside the missing imagery.
- **False positives**: `buried-raster` (4×) — flags the `.grain` SVG-noise texture, but that element explicitly sets `mix-blend-mode:overlay`, which the rule's own exemption text covers ("let the material show ... a blend mode"); this looks like the detector not honoring its own blend-mode exemption. `cream-palette` (1×) — the warm cream background is a deliberate, client-approved editorial choice, not default AI beige; low confidence this is a real defect in context. `nested-cards` (6×) — most likely firing on the `.deviceCase > .sceneFrame > .viewport` bezel-framing structure, which is a device mockup, not duplicate content cards; the JSON gives no distinguishing detail to confirm, flagged as likely FP.

**Visual overlays**: browser injection succeeded (real DOM mutation confirmed, `detect.js` loaded and ran, console captured); no persistent `[Human]` tab is available in this session to hand back, so overlays are not currently visible in an open browser tab — only the console evidence above.

#### Overall Impression

The copy voice is the strongest thing on the page and genuinely answers your "feels AI-made" worry — it reads like someone who knows this exact product wrote it. But the visual frame around that copy is a generic, repeatable SaaS-tour skeleton with no wedding-specific material, which is exactly what a visitor notices *before* reading the copy. The single best idea on the page — the twin-phone "Blush/Slate" section — proves the team knows how to *show* rather than *tell*; that instinct needs to reach the other 6 sections.

#### What's Working

- **Copy voice**: specific, confident, product-literate — the strongest evidence against "generic AI site."
- **Craft details a template wouldn't produce**: `text-wrap:pretty`, `backdrop-filter:blur(16px)` header, the SVG grain texture, muted-text colors tuned for contrast (where they pass) instead of flat gray.
- **The twin-phone "1 boda · 2 móviles" section**: visualizes the core value prop instead of describing it, and lands right before the close — a real emotional peak.

#### Priority Issues

**[P1] Sticky header "Acceder" link's real tap target is a fraction of its visible button** — Why it matters: this is the one persistent conversion element visible through the entire 8686px scroll; the visible bordered/padded box measures 79.6×27.5px but the actual clickable `<a>` inside is only 53.6×10px (padding sits on a non-interactive wrapping `<span>`, not the link) — tapping the visible button area mostly does nothing. — Fix: move the border/padding onto the `<a>` itself (`display:inline-block`), not the wrapper. — Suggested command: `/impeccable harden`

**[P1] Generic SaaS-tour visual skeleton; no wedding-specific imagery anywhere** — Why it matters: this is precisely the "denota creado por IA" risk named up front — a visitor's first visual impression, before any copy is read, is a reskinnable app-tour template. — Fix: introduce 2-3 moments of real domain-specific visual material (invitation-paper texture, a wax-seal/ribbon motif, real environmental photography at a bezel edge) so the page can't be copy-swapped to another vertical without visibly breaking. — Suggested command: `/impeccable bolder`

**[P1] Low-contrast text on the blush background fails WCAG AA** — Why it matters: `--muted` on `--blush` measures 4.4:1 and `--sage` on `--blush` measures 3.7:1, both under the 4.5:1 floor for body text — real accessibility failure, not a style nitpick. — Fix: darken `--muted`/`--sage` specifically for use on `--blush`, or add a dedicated on-blush token. — Suggested command: `/impeccable audit`

**[P1] Eyebrow/kicker above every section heading — a banned pattern that reinforces the template read** — Why it matters: repeated identically 9 times (hero + 6 chapters + sync + final CTA), this is the single most template-coded structural habit on the page. — Fix: let headings carry their own weight; fold the "chapter number" wayfinding into the numeral treatment already in place instead of a separate label line. — Suggested command: `/impeccable typeset`

**[P2] Guide-chip touch targets are 29.5px tall with 6px gaps** — Why it matters: this is the mechanism for exploring every chapter's real product screens — the page's core proof — so precision-dependent mis-taps directly undermine the demo. Corroborated by the detector's cramped-padding/undersized-ui-text hits. — Fix: raise to ~40-44px min-height, widen gaps to 8-10px. — Suggested command: `/impeccable adapt`

**[P2] Up to 9 simultaneous, equal-weight chips per section with no progressive disclosure** — Why it matters: Invitación alone shows 9 options with no primary/secondary distinction; a skimming visitor doesn't know where to start. — Fix: default to a "top 3" set with a "+ más" expansion. — Suggested command: `/impeccable layout`

**[P2] No pricing or trust content anywhere on a commercial landing page** — Why it matters: every CTA leads straight to a login gate with zero information about cost or commitment — a first-time visitor has no basis to click through. — Fix: add a compact trust/pricing block before the final CTA. — Suggested command: `/impeccable clarify`

**[P3] Long all-caps eyebrow lines hurt legibility** (e.g. "Para dos personas · una sola boda", "1 boda · 2 móviles · 2 personalizaciones") — Fix: shorten or drop the uppercase transform on the longer ones. — Suggested command: `/impeccable typeset`

#### Persona Red Flags

**Jordan (first-timer)**: no pricing/trust content means no basis to proceed past "Acceder"; the 9-chip Invitación row is jargon-dense with no guided starting point; the bare "No se ha podido cargar X." failure text (which will occasionally fire for real on flaky mobile connections in production, not just this sandbox) reads as "this is broken" on a first impression.

**Riley (stress-tester)**: the header CTA's fake hit-zone is exactly the kind of "the button doesn't work" report Riley would file; rapid-tapping chips creates a window where the caption text (updates on click) can visibly disagree with the still-loading iframe (updates on `load`) for up to a second.

**Casey (distracted mobile)**: 8686px of scroll through 6 structurally identical chapters gives her nothing to visually latch onto while skimming — by chapter 3 it's fully predictable; the small chips are exactly the target that produces mis-taps one-handed; no way to jump straight to the one chapter she cares about.

#### Minor Observations

- Two different "waiting" strings for the same state ("Abriendo esa función…" vs "Cargando X…") — pick one voice.
- `.guide button` has no `aria-pressed`/`aria-current` — the "on" state is visual-only for assistive tech.
- Footer "Wedding Digital Tools" vs. header "Weddly / SMART DESIGN" vs. final-CTA "WEDDLY SMART DESIGN" — three slightly different brand-name treatments cluster in the last two screens.
- Hero device height (520px) vs. feature-section device height (560px) — minor rhythm inconsistency.
- 13px italic `.caption` — italics reduce legibility further at small sizes, worth checking against an older audience.
- Logo asset appears to carry its own light background card rather than being matted transparent, creating a visible rectangular artifact on the dark hero — worth a source-asset check.

#### Questions to Consider

1. If you cropped out all the copy and showed someone 3 random sections' palette + typography + bezel treatment, could they tell this is a wedding product rather than a shared trip-planning app? What's the one visual element that's actually *ownable* to weddings, beyond a color choice?
2. The real proof here is 9 live demo screens across 6 chapters — right now gated behind small text chips a visitor has to tap through in sequence. What if the 3-4 strongest screens told the story instead of the chrome-and-chips scaffold?
3. If forced to keep only each chapter's single most convincing screen, which would you choose — and would the page convert better at half the length?
